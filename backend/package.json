{
  "name": "volume-backend",
  "version": "1.0.0",
  "description": "Simple Express backend for storing contact messages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
